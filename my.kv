#:kivy 1.0
#:import main HomeHelpService_API
#:import ListAdapter kivy.adapters.listadapter.ListAdapter
#:import ListItemButton kivy.uix.listview.ListItemButton

<LoginWidget>:
  BoxLayout:
    size: root.width - 200, root.height - 200
    pos:100, 100
    Button:
      text: 'Login'
      on_press: root.manager.current = 'data'

# <AssignmentsList>:
#   name:'AssignmentsList'
#   # orientation: 'vertical'
#   # BoxLayout:
#   #   size: root.width - 200, root.height - 200
#   #   pos:100, 100
#     # Button:
#     #   text: 'Exit'
#     #   on_press: parent.manager.current = 'login'
    
<ViewMap>:
  name: 'ViewMap'
  id: 'ViewMap'


<DataScreen>:
  name:'data'
  # AssignmentsList:
  BoxLayout:
    orientation: 'vertical'
    BoxLayout:
      orientation: 'horizontal'
      BoxLayout:
        AssignmentsList:
      BoxLayout:
        ViewMap:
    # BoxLayout:
    #   Button:
    #     text: 'Exit'
